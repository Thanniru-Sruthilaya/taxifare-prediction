# -*- coding: utf-8 -*-
"""project1.ipynb

Automatically generated by Colaboratory.

Original file is located at
    https://colab.research.google.com/drive/1IxXN9-BAltQFuqPPFVS4Ohq0lyKl01q4
"""

import numpy as np # linear algebra
import pandas as pd # data processing, CSV file I/O (e.g. pd.read_csv)

import os
for dirname, _, filenames in os.walk('/kaggle/input'):
    for filename in filenames:
        print(os.path.join(dirname, filename))

import sklearn
import seaborn as sns
import matplotlib.pyplot as plt
from sklearn.model_selection import  train_test_split
import xgboost as xgb
from sklearn.preprocessing import StandardScaler
from sklearn.metrics import accuracy_score
from sklearn.metrics import mean_squared_error as MSE

df.sample()

df.sample(10)

df.tail()

df.dtypes

df.describe(include="all")



df.info()

df.head()

df.describe()

train_Y = np.array(df['amount'])

df['distance_2D'] = (
    (df['longitude_of_dropoff'] - df['longitude_of_pickup']) ** 2 + \
    (df['latitude_of_pickup'] - df['latitude_of_pickup']) ** 2) ** 0.5


df = df.drop(['longitude_of_pickup', 
              'latitude_of_pickup', 
              'longitude_of_dropoff', 
              'latitude_of_dropoff', 
              'date_time_of_pickup'
              ], axis=1)
              
df.head()









